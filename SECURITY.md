# 安全策略 / Security Policy

## 支持的版本

| 版本 | 是否支持 |
|------|----------|
| 0.3.x | 支持 |
| < 0.3 | 不再维护，建议升级 |

## 报告漏洞

发现安全问题，别直接提公开 issue。

1. 私下联系维护者：提 issue 时选 `Security` 或邮件至仓库 owner（见 GitHub profile）。
2. 描述：影响范围、复现步骤、PoC（若有）、建议修复。
3. 我们会在 3 个工作日内确认，14 天内给出修复计划。

> [!IMPORTANT]
> 漏洞修复前请勿公开细节。修复发布后我们会在 CHANGELOG 与 Release Notes 中致谢（若你愿意具名）。

## 已知注意点

- `ADMIN_TOKEN` 必须改成强随机串，默认只监听 `127.0.0.1`，别暴露到公网。
- 密钥与渠道 API Key 存于本地 SQLite `data/localapi.db`，妥善保管该文件权限。
- 余额 `live` 来自上游 billing 接口，失败回退为本地估算，仅供参考。

## 隐私

本项目不上传任何密钥或用量到第三方。所有数据留在你的 `DATA_DIR`。
