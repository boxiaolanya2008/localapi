{
  "name": "localapi",
  "version": "0.4.0",
  "private": true,
  "description": "本地 API 中转站:OpenAI 兼容中转 + Vue 管理台 / Local OpenAI-compatible API relay with Vue admin dashboard",
  "repository": {
    "type": "git",
    "url": "https://github.com/boxiaolanya2008/localapi.git"
  },
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm --dir server build && pnpm --dir web build",
    "build:desktop": "pnpm --dir server build && pnpm --dir web build && pnpm --dir desktop build",
    "dist:desktop": "pnpm build:desktop && pnpm --dir desktop dist:dir",
    "desktop": "pnpm --dir desktop dev",
    "start": "pnpm --dir server start",
    "test": "pnpm --dir server test",
    "watch": "node tools/watch.mjs",
    "demo": "node tools/mock-upstream.mjs"
  },
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=20"
  },
  "license": "GPL-3.0"
}