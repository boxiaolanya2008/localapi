# 本地 API 中转站配置。渠道/模型/密钥全部在管理界面配置,这里只放服务级参数
PORT=3000
HOST=127.0.0.1

# 管理网页登录令牌(防局域网手滑访问,必须改成你自己的)
ADMIN_TOKEN=change-me-to-a-long-random-string

# 可选:模型名映射 本地名=上游名,逗号分隔;留空原样转发
MODEL_MAP=

# 数据目录(存 SQLite),留空默认 <项目根>/data
DATA_DIR=