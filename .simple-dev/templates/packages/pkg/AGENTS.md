# AGENTS.md — Package: @scope/pkg-name

## Overview / 概述

Package description.

## Dependencies / 依赖

- `@deepseek-ai/cordis`: peerDependency
- `@deepseek-ai/dsh-brand`: workspace:*

## Exports / 导出

- `MyService` - Service class (default)
- `Types` - TypeScript types (named)

## Internal Modules / 内部模块

- `src/service.ts` - Service implementation
- `src/types.ts` - Type definitions

## Tests / 测试

Run from package root:
```bash
pnpm test
```

## Coding Rules / 编码规则

- No ornamental comments / 禁止装饰注释
- No emoji / 禁止emoji
- Minimal syntax / 语法极简
