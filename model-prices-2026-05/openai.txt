厂商: OpenAI
来源: https://platform.openai.com/pricing
抓取状态: 未能自动获取（HTTP 403）。

价格（2026年5月）
- 输入价格（input price）: 未获取（需手动确认）
- 输出价格（output price）: 未获取（需手动确认）
- 缓存写入（cache write）: 视为与输入相同 -> 未获取（需手动确认）
- 缓存读取（cache read）: 视为与输出相同 -> 未获取（需手动确认）

说明:
- 自动抓取返回 403（无法访问/需要额外授权）。请使用有权限的网络或提供页面截图/文本以便填充。